# Banc de prototypage de stratégies (Sprint 8). Le moteur de PRODUCTION reste le
# C++ (../include) ; ce dossier ne sert qu'à itérer vite des idées de stratégie.

# Requis (swingbench tourne avec ça seul) :
numpy
pandas<3            # vectorbt 1.x exige pandas 2.x ; swingbench tourne sur les deux

# Backtest vectorisé (balayages/heatmaps rapides via vbt_adapter.py) :
vectorbt            # installe numba/llvmlite/plotly ; vérifié OK avec QQQ.csv

# Optionnel (tearsheets HTML) :
# quantstats
